define( [ 'ol' ], function ( ol ) {

    // Class
    function Overview( map ) {
        this.map = map;
        
    }

    Overview.prototype.addLayer = function () {
    }

    return Overview;

});
